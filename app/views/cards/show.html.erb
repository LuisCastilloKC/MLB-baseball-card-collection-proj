<h1><%= @card.player_name %></h1>
<h2>By User: <%= @card.user.username %></h2>

<div> <%= @card.team_name %></div>

<p> <%= link_to 'See all comments', card_comments_path(@card) %></p>
<p> <%= link_to 'New comments', new_card_comment_path(@card) %></p>

<h4>Comments:</h4>
<% @card.comments.each do |c| %>
    <li><strong> <%= c.user.username%></strong> say "<%= link_to c.content, card_comment_path(@card, c)%>" </li>
<% end %>